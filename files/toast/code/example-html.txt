<div class="toast">
  <div class="toast__icon">
    <app-icon [name]="iconName()" [size]="18" />
  </div>
  <div class="toast__content">
    <h3 class="toast__title">{{ displayTitle() }}</h3>
    <p class="toast__text">{{ message() }}</p>
  </div>
  <button
    type="button"
    class="toast__close"
    (click)="onClose()"
    aria-label="Close notification"
  >
    <app-icon name="close" />
  </button>
</div>
