.ripple-wrap {
  position: relative;
  overflow: hidden;
}

.ripple {
  border-radius: 50%;
  background: rgba(205, 205, 205, 0.40);
  transform: scale(0);
  position: absolute;
  opacity: 1;
  animation: ripple 0.4s ease-out forwards;
}

@keyframes ripple {
  to {
    transform: scale(2);
    opacity: 0;
  }
}
