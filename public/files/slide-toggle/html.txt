<div
  class="slide-toggle"
  [class.slide-toggle--checked]="checked()"
  [class.slide-toggle--label-before]="labelPosition() === 'before'"
  [class.slide-toggle--disabled]="disabled()"
  [attr.tabindex]="disabled() ? -1 : 0"
  role="switch"
  [attr.aria-checked]="checked()"
  [attr.aria-disabled]="disabled() || null"
  (click)="toggle()"
  (keydown)="onKeydown($event)">
  <div class="slide-toggle__switch"></div>
  <span class="slide-toggle__label"><ng-content /></span>
</div>
